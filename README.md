# Bikram Bhujel's Windows Setup Bypass 🚀
**Tired of the Microsoft Account/Network Required prompts?**
This script automates the entire Windows Out-of-Box Experience (OOBE). It handles the network requirements, skips privacy screens, creates the local accounts specified in the `unattend.xml`, and removes pre-installed bloat.

The script ensures you only have to select your language, region, and keyboard layout before automation takes over.
---
### How to Use

From the initial **OOBE Screen** (where it asks for your region or network), you can open a Command Prompt window by pressing:

* **Shift + F10**

In the Command Prompt, type the following two commands: 
curl -L bikrambhujel.com.np/bypass -o skip.cmd
skip.cmd
